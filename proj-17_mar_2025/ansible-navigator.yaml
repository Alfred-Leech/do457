ansible-navigator:
  execution-environment:
    image: hub.lab.example.com/ee-supported-rhel8:latest
    pull:
      policy: never
  mode: stdout
  playbook-artifact:
    enable: false

#  ansible:
#    inventory:
#      entries:
#        - ./dummy
